# Branching audio/video
ffmpeg -i $1 -map 0:v -vcodec copy video/`basename $1`.ogv -map 0:a -acodec copy audio/`basename $1`.ogg
input
audio video
# Audio branch
equalizer
audio
equalized
# Video branch
# Branching shaky xor not
is_shaky $1 shaky stabilized
video
shaky stabilized
# Video branch/shaky branch
stabilize
shaky
stabilized
# (Shaky xor not) merged in stabilized
# Merge audio and video branches in output
ffmpeg -i $1 -i $2 -c copy output/`basename $1`.ogg
equalized stabilized
(.*).og[gv]$
output

